name: stories_page_view_example
description: Example of a Stories Page View using the [stories_page_view](https://pub.dev/packages/stories_page_view) package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  stories_page_view: ^0.0.4
  video_player: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
